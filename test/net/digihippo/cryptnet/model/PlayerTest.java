package net.digihippo.cryptnet.model;

import org.junit.Test;

import java.util.Arrays;

import static org.hamcrest.CoreMatchers.equalTo;
import static org.junit.Assert.assertThat;

public class PlayerTest
{
//    private final Segment segmentOne = Segment.createLine(new Pixel(0, 0), new Pixel(10, 10));
//    private final Segment segmentTwo = Segment.createLine(new Pixel(10, 10), new Pixel(20, 20));
//    private final Segment segmentThree = Segment.createLine(new Pixel(20, 20), new Pixel(30, 30));
//    private final Segment segmentFour = Segment.createLine(new Pixel(30, 30), new Pixel(40, 40));
//    private final Path path = new Path(Arrays.asList(segmentOne, segmentTwo, segmentThree, segmentFour));
//
//    @Test
//    public void roundTrip()
//    {
//        Player player = new Player(path, segmentTwo, new DoublePoint(13, 13));
//
//        assertThat(Player.parse(player.toString()), equalTo(player));
//    }
}